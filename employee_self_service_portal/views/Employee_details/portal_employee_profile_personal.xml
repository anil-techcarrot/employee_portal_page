<odoo>
    <template id="portal_employee_profile_personal">
        <t t-call="employee_self_service_portal.portal_employee_profile_base">
            <t t-set="section" t-value="'personal'"/>
            <t t-set="section_content">

                <t t-set="all_countries" t-value="request.env['res.country'].sudo().search([], order='name')"/>

                <!-- Success/Error Messages -->
                <div id="messageContainer" style="display: none;">
                    <div id="successMessage" class="alert alert-success alert-dismissible fade" role="alert"
                         style="display: none;">
                        <i class="fa fa-check-circle me-2"></i>
                        <span id="successText"></span>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                    <div id="errorMessage" class="alert alert-danger alert-dismissible fade" role="alert"
                         style="display: none;">
                        <i class="fa fa-exclamation-triangle me-2"></i>
                        <span id="errorText"></span>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                </div>

                <!-- ===== MAIN FORM ===== -->
                <form id="personalDetailsForm" action="/my/employee/personal" method="post"
                      enctype="multipart/form-data">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                    <!-- ===== TAB NAVIGATION ===== -->
                    <ul class="nav nav-tabs mb-4 profile-tab-navigation" role="tablist">
                        <li class="nav-item" role="presentation">
                            <button type="button" class="nav-link profile-tab-button active"
                                    data-tab-target="basic" aria-selected="true">
                                <i class="fa fa-user me-1"></i>Basic Info
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button type="button" class="nav-link profile-tab-button"
                                    data-tab-target="professional" aria-selected="false">
                                <i class="fa fa-briefcase me-1"></i>Professional
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button type="button" class="nav-link profile-tab-button"
                                    data-tab-target="family" aria-selected="false">
                                <i class="fa fa-users me-1"></i>Family
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button type="button" class="nav-link profile-tab-button"
                                    data-tab-target="education" aria-selected="false">
                                <i class="fa fa-graduation-cap me-1"></i>Education
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button type="button" class="nav-link profile-tab-button"
                                    data-tab-target="documents" aria-selected="false">
                                <i class="fa fa-folder-open me-1"></i>Documents
                            </button>
                        </li>
                    </ul>

                    <!-- ============================================================ -->
                    <!--  TAB: BASIC INFO                                              -->
                    <!-- ============================================================ -->

                    <!-- Basic Information -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="basic">
                        <div class="card-header bg-light d-flex justify-content-between align-items-center">
                            <h5 class="mb-0">
                                <i class="fa fa-user text-primary me-2"></i>Basic Information
                            </h5>
                            <div class="form-check form-switch">
                                <input class="form-check-input edit-mode-toggle" type="checkbox"
                                       id="editModeBasic" onchange="syncAndToggleEditMode(this)"/>
                                <label class="form-check-label" for="editModeBasic">Edit Mode</label>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <!-- NAME FIELDS: text only, always-readonly -->
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">Employee First Name</label>
                                    <input type="text" name="employee_first_name" class="form-control"
                                           t-att-value="employee.employee_first_name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">Employee Middle Name</label>
                                    <input type="text" name="employee_middle_name" class="form-control"
                                           t-att-value="employee.employee_middle_name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">Employee Last Name</label>
                                    <input type="text" name="employee_last_name" class="form-control"
                                           t-att-value="employee.employee_last_name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Employee Name (English)</label>
                                    <input type="text" name="employee_name_english" class="form-control"
                                           t-att-value="employee.employee_name_english or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Employee Name (Arabic)</label>
                                    <input type="text" name="employee_name_arabic" class="form-control"
                                           t-att-value="employee.employee_name_arabic or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Employment Type</label>
                                    <input type="text" name="employment_type" class="form-control"
                                           t-att-value="employee.employment_type or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Engagement Location</label>
                                    <input type="text" name="engagement_location" class="form-control"
                                           t-att-value="employee.engagement_location or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Payroll</label>
                                    <input type="text" name="payroll_location" class="form-control"
                                           t-att-value="employee.payroll_location or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Currency</label>
                                    <input type="text" name="currency_id" class="form-control"
                                           t-att-value="employee.currency_id.name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Nationality At Birth</label>
                                    <input type="text" name="nationality_at_birth_id" class="form-control"
                                           t-att-value="employee.nationality_at_birth_id.name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <!-- WORK EMAIL: email validation -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-envelope text-muted me-1"></i>Work Email
                                        <span class="text-danger">*</span>
                                    </label>
                                    <input type="email" name="work_email" class="form-control"
                                           t-att-value="employee.work_email"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="email"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- WORK PHONE: digits + optional + only -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-phone text-muted me-1"></i>Work Phone
                                    </label>
                                    <input type="tel" name="work_phone" class="form-control"
                                           t-att-value="employee.work_phone"
                                           readonly="readonly"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only, optional leading + for country code</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Nationality</label>
                                    <input class="form-control"
                                           t-att-value="employee.country_id.name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-venus-mars text-muted me-1"></i>Gender
                                    </label>
                                    <select name="sex" class="form-select"
                                            data-always-readonly="1" readonly="readonly" disabled="">
                                        <option value="">Select Gender</option>
                                        <option value="male"
                                                t-att-selected="'selected' if employee.sex == 'male' else None">Male
                                        </option>
                                        <option value="female"
                                                t-att-selected="'selected' if employee.sex == 'female' else None">Female
                                        </option>
                                        <option value="other"
                                                t-att-selected="'selected' if employee.sex == 'other' else None">Other
                                        </option>
                                    </select>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- DATE OF BIRTH: past date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-calendar text-muted me-1"></i>Date of Birth
                                    </label>
                                    <input type="date" name="birthday" class="form-control"
                                           t-att-value="employee.birthday"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Personal Information -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="basic">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-info-circle text-primary me-2"></i>Personal Information
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Legal Name</label>
                                    <input name="legal_name" class="form-control"
                                           t-att-value="employee.legal_name or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Place of Birth</label>
                                    <input name="place_of_birth" class="form-control"
                                           t-att-value="employee.place_of_birth or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Payslip Language</label>
                                    <input class="form-control"
                                           t-att-value="employee.lang or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Blood Group</label>
                                    <select name="blood_group" class="form-select" disabled="disabled">
                                        <option value="">-- Select Blood Group --</option>
                                        <option value="a_pos"
                                                t-att-selected="'selected' if employee.blood_group == 'a_pos' else None">
                                            A+
                                        </option>
                                        <option value="a_neg"
                                                t-att-selected="'selected' if employee.blood_group == 'a_neg' else None">
                                            A-
                                        </option>
                                        <option value="b_pos"
                                                t-att-selected="'selected' if employee.blood_group == 'b_pos' else None">
                                            B+
                                        </option>
                                        <option value="b_neg"
                                                t-att-selected="'selected' if employee.blood_group == 'b_neg' else None">
                                            B-
                                        </option>
                                        <option value="ab_pos"
                                                t-att-selected="'selected' if employee.blood_group == 'ab_pos' else None">
                                            AB+
                                        </option>
                                        <option value="ab_neg"
                                                t-att-selected="'selected' if employee.blood_group == 'ab_neg' else None">
                                            AB-
                                        </option>
                                        <option value="o_pos"
                                                t-att-selected="'selected' if employee.blood_group == 'o_pos' else None">
                                            O+
                                        </option>
                                        <option value="o_neg"
                                                t-att-selected="'selected' if employee.blood_group == 'o_neg' else None">
                                            O-
                                        </option>
                                        <option value="unknown"
                                                t-att-selected="'selected' if employee.blood_group == 'unknown' else None">
                                            Unknown
                                        </option>
                                    </select>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Religion</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.religion.name if employee.religion else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Identity Documents -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="basic">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-id-card text-primary me-2"></i>Identity Documents
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <!-- EMIRATES ID: digits-and-dashes format only -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-id-badge text-muted me-1"></i>Emirates ID
                                    </label>
                                    <input type="text" name="emirates_id_number" class="form-control"
                                           t-att-value="employee.emirates_id_number"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="emirates-id"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Format: 784-1234-1234567-1</small>
                                </div>
                                <!-- EMIRATES EXPIRY: must be future date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-calendar-times text-muted me-1"></i>Emirates ID Expiry
                                    </label>
                                    <input type="date" name="emirates_expiry_date" class="form-control"
                                           t-att-value="employee.emirates_expiry_date"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="future-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- PASSPORT NUMBER: alphanumeric, no spaces -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-passport text-muted me-1"></i>Passport Number
                                    </label>
                                    <input type="text" name="passport_id" class="form-control"
                                           t-att-value="employee.passport_id"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="alphanumeric"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Letters and digits only, no spaces</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-globe text-muted me-1"></i>Passport Issuing Country
                                    </label>
                                    <input type="text" name="issue_countries_id" class="form-control"
                                           t-att-value="employee.issue_countries_id.name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- PASSPORT ISSUE DATE: must be past -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-calendar text-muted me-1"></i>Passport Issue Date
                                    </label>
                                    <input type="date" name="issue_date" class="form-control"
                                           t-att-value="employee.issue_date"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- PASSPORT EXPIRY: must be future -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-calendar-times text-muted me-1"></i>Passport Expiry Date
                                    </label>
                                    <input type="date" name="expiry_date" class="form-control"
                                           t-att-value="employee.expiry_date.strftime('%Y-%m-%d') if employee.expiry_date else ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="future-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- IDENTIFICATION NO: alphanumeric -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Identification No</label>
                                    <input name="identification_id" class="form-control"
                                           t-att-value="employee.identification_id or ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="alphanumeric"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Letters and digits only</small>
                                </div>
                                <!-- SSN: digits only -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">SSN No</label>
                                    <input name="ssnid" class="form-control"
                                           t-att-value="employee.ssnid or ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="digits-only"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Non Resident</label>
                                    <div class="form-check mt-2">
                                        <input type="checkbox" class="form-check-input"
                                               name="is_non_resident" id="is_non_resident"
                                               t-att-checked="'checked' if employee.is_non_resident else None"
                                               disabled="disabled"/>
                                        <label class="form-check-label" for="is_non_resident">
                                            <t t-if="employee.is_non_resident">Yes</t>
                                            <t t-else="">No</t>
                                        </label>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Visa and Work Permit -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="basic">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-file-text text-primary me-2"></i>Visa &amp; Work Permit
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <!-- VISA NO: alphanumeric -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Visa No</label>
                                    <input name="visa_no" class="form-control"
                                           t-att-value="employee.visa_no or ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="alphanumeric"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Letters and digits only</small>
                                </div>
                                <!-- PERMIT NO: alphanumeric -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Work Permit No</label>
                                    <input name="permit_no" class="form-control"
                                           t-att-value="employee.permit_no or ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="alphanumeric"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Letters and digits only</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Work Permit File</label>
                                    <t t-if="employee.has_work_permit">
                                        <div class="mb-2">
                                            <a t-att-href="'/web/content/hr.employee/%s/has_work_permit?download=true' % employee.id"
                                               target="_blank" class="btn btn-sm btn-outline-primary">
                                                <i class="fa fa-download me-1"></i>Download Current File
                                            </a>
                                        </div>
                                    </t>
                                    <input type="file" name="has_work_permit" class="form-control"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Contact Information -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="basic">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-address-book text-primary me-2"></i>Contact Information
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <!-- PERSONAL EMAIL -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-envelope text-muted me-1"></i>Personal Email
                                    </label>
                                    <input type="email" name="private_email" class="form-control"
                                           t-att-value="employee.private_email"
                                           readonly="readonly"
                                           data-validation="email"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- PERSONAL PHONE: digits only + optional + -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-phone text-muted me-1"></i>Personal Phone
                                    </label>
                                    <input type="tel" name="private_phone" class="form-control"
                                           t-att-value="employee.private_phone"
                                           readonly="readonly"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only, optional leading + for country code</small>
                                </div>
                                <!-- WHATSAPP: digits only + optional + -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">WhatsApp</label>
                                    <input type="text" name="whatsapp" class="form-control"
                                           t-att-value="employee.whatsapp or ''"
                                           readonly="readonly"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only, optional leading + for country code</small>
                                </div>
                                <div class="col-md-12">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-map-marker text-muted me-1"></i>Address Line 1
                                    </label>
                                    <input type="text" name="private_street" class="form-control"
                                           t-att-value="employee.private_street"
                                           readonly="readonly"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-12">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-map-marker text-muted me-1"></i>Address Line 2
                                    </label>
                                    <input type="text" name="private_street2" class="form-control"
                                           t-att-value="employee.private_street2"
                                           readonly="readonly"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">City</label>
                                    <input type="text" name="private_city" class="form-control"
                                           t-att-value="employee.private_city"
                                           readonly="readonly"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">State/Province</label>
                                    <input type="text" name="private_state_id" class="form-control"
                                           t-att-value="employee.private_state_id.name"
                                           readonly="readonly"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- ZIP: digits only -->
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">ZIP/Postal Code</label>
                                    <input type="text" name="private_zip" class="form-control"
                                           t-att-value="employee.private_zip"
                                           readonly="readonly"
                                           data-validation="digits-only"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Bank Accounts</label>
                                    <input type="text" class="form-control"
                                           t-att-value="', '.join(employee.bank_account_ids.mapped('acc_number')) if employee.bank_account_ids else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Social Media -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="basic">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-share-alt text-primary me-2"></i>Social Media Details
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <!-- Social media: text/URL fields, no integer validation needed -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">LinkedIn</label>
                                    <input type="text" name="linkedin" class="form-control"
                                           t-att-value="employee.linkedin or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Facebook Profile</label>
                                    <input type="text" name="facebook_profile" class="form-control"
                                           t-att-value="employee.facebook_profile or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Instagram Profile</label>
                                    <input type="text" name="insta_profile" class="form-control"
                                           t-att-value="employee.insta_profile or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Twitter Profile</label>
                                    <input type="text" name="twitter_profile" class="form-control"
                                           t-att-value="employee.twitter_profile or ''"
                                           readonly="readonly"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- ============================================================ -->
                    <!--  TAB: PROFESSIONAL                                            -->
                    <!-- ============================================================ -->

                    <!-- Work Information -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="professional">
                        <div class="card-header bg-light d-flex justify-content-between align-items-center">
                            <h5 class="mb-0">
                                <i class="fa fa-briefcase text-primary me-2"></i>Work Information
                            </h5>
                            <div class="form-check form-switch">
                                <input class="form-check-input edit-mode-toggle" type="checkbox"
                                       id="editModeProfessional" onchange="syncAndToggleEditMode(this)"/>
                                <label class="form-check-label" for="editModeProfessional">Edit Mode</label>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Company</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.company_id.name"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Employee Code</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.emp_code"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Department</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.department_id.name"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Job Position</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.job_id.name"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Job Title</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.job_title"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Account Manager</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.parent_id.name"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Employment Status</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.employment_status_id.name if employee.employment_status_id else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <!-- NOTICE PERIOD: digits only (number of days) -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Notice Period (in days)</label>
                                    <input type="text" name="notice_period" class="form-control"
                                           t-att-value="employee.notice_period or ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="digits-only"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">DOJ</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.doj or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Original Hire Date</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.original_hire_date or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Resignation Date</label>
                                    <input type="date" class="form-control"
                                           t-att-value="employee.resign_date.strftime('%Y-%m-%d') if employee.resign_date else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">End Date</label>
                                    <input type="date" class="form-control"
                                           t-att-value="employee.end_date.strftime('%Y-%m-%d') if employee.end_date else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">LWD</label>
                                    <input type="date" class="form-control"
                                           t-att-value="employee.lwd.strftime('%Y-%m-%d') if employee.lwd else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Work Location -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="professional">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-map-marker text-primary me-2"></i>Work Location
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Work Address</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.address_id.name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Work Location</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.work_location_id.name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Full Address Inside UAE</label>
                                    <input type="text" name="u_private_city" class="form-control"
                                           t-att-value="employee.u_private_city or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Current Work Address</label>
                                    <input type="text" name="current_address" class="form-control"
                                           t-att-value="employee.current_address or ''"
                                           readonly="readonly"/>
                                </div>
                                <!-- ISD CODE: digits + optional + -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">ISD Code</label>
                                    <input type="text" name="phone_code_1" class="form-control"
                                           t-att-value="employee.phone_code_1 or ''"
                                           readonly="readonly"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">E.g. +91, +971</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">House Number and Building Name</label>
                                    <input type="text" name="house_no" class="form-control"
                                           t-att-value="employee.house_no or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Area / Town Name</label>
                                    <input type="text" name="area_name" class="form-control"
                                           t-att-value="employee.area_name or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">City</label>
                                    <input type="text" name="city" class="form-control"
                                           t-att-value="employee.city or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Country of Residency</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.country_residences_id.name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Country</label>
                                    <input type="text" name="countries_id" class="form-control"
                                           t-att-value="employee.countries_id.name or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">State</label>
                                    <input type="text" name="states_id" class="form-control"
                                           t-att-value="employee.states_id.name or ''"
                                           readonly="readonly"/>
                                </div>
                                <!-- ZIP CODE: digits only -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Zip Code</label>
                                    <input type="text" name="zip_code" class="form-control"
                                           t-att-value="employee.zip_code or ''"
                                           readonly="readonly"
                                           data-validation="digits-only"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only</small>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- General Information -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="professional">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-info-circle text-primary me-2"></i>General Information
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Practice</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.practice.name if employee.practice else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Sub Practice</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.sub_practice.name if employee.sub_practice else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Practice Head</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.practice_heads_id.name if employee.practice_heads_id else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Employee Inside UAE</label>
                                    <input type="text" class="form-control"
                                           t-att-value="'Yes' if employee.emp_inside_uae else 'No'"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Industry Start Date</label>
                                    <input type="date" name="industry_start_date" class="form-control"
                                           t-att-value="employee.industry_start_date.strftime('%Y-%m-%d') if employee.industry_start_date else ''"
                                           readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Experience</label>
                                    <input type="text" name="experience" class="form-control"
                                           t-att-value="employee.experience or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Current / Additional Role</label>
                                    <input type="text" name="current_role" class="form-control"
                                           t-att-value="employee.current_role or ''"
                                           readonly="readonly"/>
                                </div>
                                <!-- AADHAR: 12 digits only -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Aadhar Number</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.aadhar_no or ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="aadhar"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">12 digits only</small>
                                </div>
                                <!-- PAN: alphanumeric, 10 chars -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">PAN</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.pan or ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="pan"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Format: ABCDE1234F (10 characters)</small>
                                </div>
                                <!-- UAN: 12 digits -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">UAN</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.uan or ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="digits-only"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only</small>
                                </div>
                                <!-- PF NUMBER: alphanumeric -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">PF Number</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.pf_number or ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="alphanumeric"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Letters and digits only</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Payroll</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.payroll or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Mentor Name</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.mentor_names_id.name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Last Organisation Info -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="professional">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-building text-primary me-2"></i>Last Organisation Info
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Organisation Name</label>
                                    <input type="text" name="last_organisation_name" class="form-control"
                                           t-att-value="employee.last_organisation_name or ''"
                                           readonly="readonly"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Location</label>
                                    <input type="text" name="last_location" class="form-control"
                                           t-att-value="employee.last_location or ''"
                                           readonly="readonly"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Salary Per Annum Currency</label>
                                    <input type="text" name="last_salary_per_annum_currency" class="form-control"
                                           t-att-value="employee.last_salary_per_annum_currency or ''"
                                           readonly="readonly"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Salary Per Annum Amount</label>
                                    <input type="number" name="last_salary_per_annum_amt" class="form-control"
                                           t-att-value="employee.last_salary_per_annum_amt or ''"
                                           readonly="readonly"
                                           min="0" step="0.01"
                                           data-validation="positive-number"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Reason for Leaving</label>
                                    <input type="text" name="reason_for_leaving" class="form-control"
                                           t-att-value="employee.reason_for_leaving or ''"
                                           readonly="readonly"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Reporting Manager Name</label>
                                    <input type="text" name="last_report_manager_name" class="form-control"
                                           t-att-value="employee.last_report_manager_name or ''"
                                           readonly="readonly"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Reporting Manager Designation</label>
                                    <input type="text" name="last_report_manager_designation" class="form-control"
                                           t-att-value="employee.last_report_manager_designation or ''"
                                           readonly="readonly"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Last Reporting Manager Mobile Number</label>
                                    <input type="tel" name="last_report_manager_mob_no" class="form-control"
                                           t-att-value="employee.last_report_manager_mob_no or ''"
                                           readonly="readonly"
                                           pattern="[+]?[0-9\s\-\(\)]*"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Reporting Manager Email-ID</label>
                                    <input type="email" name="last_report_manager_mail" class="form-control"
                                           t-att-value="employee.last_report_manager_mail or ''"
                                           readonly="readonly"
                                           data-validation="email"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Employee Nominee and Details -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="professional">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-user-circle text-primary me-2"></i>Employee Nominee &amp; Details
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Nominee Name</label>
                                    <input type="text" name="employee_nominee_name" class="form-control"
                                           t-att-value="employee.employee_nominee_name or ''"
                                           readonly="readonly"/>
                                </div>
                                <!-- NOMINEE CONTACT: phone digits only -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Nominee Contact Number</label>
                                    <input type="tel" name="employee_nominee_contact_no" class="form-control"
                                           t-att-value="employee.employee_nominee_contact_no or ''"
                                           readonly="readonly"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only, optional leading + for country code</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Total IT Experience</label>
                                    <input type="text" name="total_it_experience" class="form-control"
                                           t-att-value="employee.total_it_experience or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Domains Worked</label>
                                    <input type="text" name="domain_worked" class="form-control"
                                           t-att-value="employee.domain_worked or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Primary Skills</label>
                                    <input type="text" name="primary_skill" class="form-control"
                                           t-att-value="employee.primary_skill or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Secondary Skills</label>
                                    <input type="text" name="secondary_skill" class="form-control"
                                           t-att-value="employee.secondary_skill or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Tools Used</label>
                                    <input type="text" name="tool_used" class="form-control"
                                           t-att-value="employee.tool_used or ''"
                                           readonly="readonly"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Career Details -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="professional">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-history text-primary me-2"></i>Career Details
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <!-- NO. OF CAREER BREAK: digits only -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">No. of Career Break</label>
                                    <input type="text" name="no_of_career_break" class="form-control"
                                           t-att-value="employee.no_of_career_break or ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="digits-only"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Career Break</label>
                                    <input type="text" name="career_break" class="form-control"
                                           t-att-value="employee.career_break or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Career Break Detail</label>
                                    <input type="text" name="career_break_detail" class="form-control"
                                           t-att-value="employee.career_break_detail or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Career Break Start Date</label>
                                    <input type="date" name="career_break_start_date" class="form-control"
                                           t-att-value="employee.career_break_start_date.strftime('%Y-%m-%d') if employee.career_break_start_date else ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Career Break End Date</label>
                                    <input type="date" name="career_break_end_date" class="form-control"
                                           t-att-value="employee.career_break_end_date.strftime('%Y-%m-%d') if employee.career_break_end_date else ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Industry Details -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="professional">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-industry text-primary me-2"></i>Industry Details
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Reference Name</label>
                                    <input type="text" name="industry_ref_name" class="form-control"
                                           t-att-value="employee.industry_ref_name or ''"
                                           readonly="readonly"/>
                                </div>
                                <!-- REFERENCE EMAIL -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Reference Email</label>
                                    <input type="email" name="industry_ref_email" class="form-control"
                                           t-att-value="employee.industry_ref_email or ''"
                                           readonly="readonly"
                                           data-validation="email"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- REFERENCE MOBILE: phone digits only -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Reference Mobile Number</label>
                                    <input type="tel" name="industry_ref_mob_no" class="form-control"
                                           t-att-value="employee.industry_ref_mob_no or ''"
                                           readonly="readonly"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only, optional leading + for country code</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Previous Company Name</label>
                                    <input type="text" name="previous_company_name" class="form-control"
                                           t-att-value="employee.previous_company_name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Designation</label>
                                    <input type="text" name="designation" class="form-control"
                                           t-att-value="employee.designation or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Period in Company</label>
                                    <input type="text" name="period_in_company" class="form-control"
                                           t-att-value="employee.period_in_company or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Reason of Leaving</label>
                                    <input type="text" name="reason_of_leaving" class="form-control"
                                           t-att-value="employee.reason_of_leaving or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Exit Type</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.exit_type_id.name if employee.exit_type_id else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Exit Reason</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.exit_reason_id.name if employee.exit_reason_id else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Home Country ID Name</label>
                                    <input type="text" name="home_country_id_name" class="form-control"
                                           t-att-value="employee.home_country_id_name or ''"
                                           readonly="readonly"/>
                                </div>
                                <!-- HOME COUNTRY ID NUMBER: alphanumeric -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Home Country ID Number</label>
                                    <input type="text" name="home_country_id_number" class="form-control"
                                           t-att-value="employee.home_country_id_number or ''"
                                           readonly="readonly"
                                           data-validation="alphanumeric"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Letters and digits only</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Customer / Account Name</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.customer_acc_name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Candidate Source</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.candidate_source or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- ============================================================ -->
                    <!--  TAB: FAMILY                                                  -->
                    <!-- ============================================================ -->

                    <!-- Emergency Contact -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="family">
                        <div class="card-header bg-light d-flex justify-content-between align-items-center">
                            <h5 class="mb-0">
                                <i class="fa fa-ambulance text-primary me-2"></i>Emergency Contact
                            </h5>
                            <div class="form-check form-switch">
                                <input class="form-check-input edit-mode-toggle" type="checkbox"
                                       id="editModeFamily" onchange="syncAndToggleEditMode(this)"/>
                                <label class="form-check-label" for="editModeFamily">Edit Mode</label>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <!-- EMERGENCY PHONE: digits only -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-phone text-muted me-1"></i>Emergency Contact Phone
                                        <span class="text-danger">*</span>
                                    </label>
                                    <input type="tel" name="emergency_phone" class="form-control"
                                           t-att-value="employee.emergency_phone"
                                           readonly="readonly" required="required"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only, optional leading + for country code</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Emergency Address</label>
                                    <input type="text" name="e_private_city" class="form-control"
                                           t-att-value="employee.e_private_city or ''"
                                           readonly="readonly"/>
                                </div>
                                <!-- COUNTRY CODE: phone format -->
                                <div class="col-md-3">
                                    <label class="form-label fw-bold">Country Code</label>
                                    <input type="text" name="phone_code_1" class="form-control"
                                           t-att-value="employee.phone_code_1 or ''"
                                           readonly="readonly"
                                           placeholder="+971"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">E.g. +971</small>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Emergency Contact Person Address Inside UAE -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="family">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-ambulance text-primary me-2"></i>Emergency Contact Person Address Inside
                                UAE
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Contact Person Name</label>
                                    <input type="text" name="emergency_contact_person_name" class="form-control"
                                           t-att-value="employee.emergency_contact_person_name or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Relationship with Employee</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.relationship_with_emp_id.name if employee.relationship_with_emp_id else ''"
                                           readonly="readonly"/>
                                </div>
                                <!-- ALL PHONE FIELDS: digits only -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Contact Person Phone Number</label>
                                    <input type="tel" name="emergency_contact_person_phone" class="form-control"
                                           t-att-value="employee.emergency_contact_person_phone or ''"
                                           readonly="readonly"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only, optional leading + for country code</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Alternate Mobile Number</label>
                                    <input type="tel" name="alternate_mobile_number" class="form-control"
                                           t-att-value="employee.alternate_mobile_number or ''"
                                           readonly="readonly"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only, optional leading + for country code</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Contact Person Name (1)</label>
                                    <input type="text" name="emergency_contact_person_name_1" class="form-control"
                                           t-att-value="employee.emergency_contact_person_name_1 or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Relationship with Employee (1)</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.second_relation_with_employee or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Contact Person Phone Number (1)</label>
                                    <input type="tel" name="emergency_contact_person_phone_1" class="form-control"
                                           t-att-value="employee.emergency_contact_person_phone_1 or ''"
                                           readonly="readonly"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only, optional leading + for country code</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Second Alternative Number</label>
                                    <input type="tel" name="second_alternative_number" class="form-control"
                                           t-att-value="employee.second_alternative_number or ''"
                                           readonly="readonly"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only, optional leading + for country code</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Home Land Line Number</label>
                                    <input type="tel" name="home_land_line_no" class="form-control"
                                           t-att-value="employee.home_land_line_no or ''"
                                           readonly="readonly"
                                           data-validation="phone"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only, optional leading + for country code</small>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Spouse Info -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="family">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-heart text-primary me-2"></i>Spouse Info
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <!-- PASSPORT NO: alphanumeric -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Spouse Passport No</label>
                                    <input type="text" name="spouse_passport_no" class="form-control"
                                           t-att-value="employee.spouse_passport_no or ''"
                                           readonly="readonly"
                                           data-validation="alphanumeric"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Letters and digits only</small>
                                </div>
                                <!-- PASSPORT ISSUE: past date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Passport Issue Date</label>
                                    <input type="date" name="spouse_passport_issue_date" class="form-control"
                                           t-att-value="employee.spouse_passport_issue_date.strftime('%Y-%m-%d') if employee.spouse_passport_issue_date else ''"
                                           readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- PASSPORT EXPIRY: future date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Passport Expiry Date</label>
                                    <input type="date" name="spouse_passport_expiry_date" class="form-control"
                                           t-att-value="employee.spouse_passport_expiry_date.strftime('%Y-%m-%d') if employee.spouse_passport_expiry_date else ''"
                                           readonly="readonly"
                                           data-validation="future-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Passport Issuing Country</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.spouse_passport_issuing_countries_id.name if employee.spouse_passport_issuing_countries_id else ''"
                                           readonly="readonly"/>
                                </div>
                                <!-- VISA NO: alphanumeric -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Visa No</label>
                                    <input type="text" name="spouse_visa_no" class="form-control"
                                           t-att-value="employee.spouse_visa_no or ''"
                                           readonly="readonly"
                                           data-validation="alphanumeric"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Letters and digits only</small>
                                </div>
                                <!-- VISA EXPIRY: future date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Visa Expiration Date</label>
                                    <input type="date" name="spouse_visa_expire_date" class="form-control"
                                           t-att-value="employee.spouse_visa_expire_date.strftime('%Y-%m-%d') if employee.spouse_visa_expire_date else ''"
                                           readonly="readonly"
                                           data-validation="future-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- EMIRATES ID: format validation -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Emirates ID Number</label>
                                    <input type="text" name="spouse_emirates_id_no" class="form-control"
                                           t-att-value="employee.spouse_emirates_id_no or ''"
                                           readonly="readonly"
                                           data-validation="emirates-id"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Format: 784-1234-1234567-1</small>
                                </div>
                                <!-- EMIRATES ISSUE: past date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Emirates Issue Date</label>
                                    <input type="date" name="spouse_emirates_issue_date" class="form-control"
                                           t-att-value="employee.spouse_emirates_issue_date.strftime('%Y-%m-%d') if employee.spouse_emirates_issue_date else ''"
                                           readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- EMIRATES EXPIRY: future date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Emirates ID Expiry Date</label>
                                    <input type="date" name="spouse_emirates_id_expiry_date" class="form-control"
                                           t-att-value="employee.spouse_emirates_id_expiry_date.strftime('%Y-%m-%d') if employee.spouse_emirates_id_expiry_date else ''"
                                           readonly="readonly"
                                           data-validation="future-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- AADHAR: 12 digits -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Aadhar Number</label>
                                    <input type="text" name="spouse_aadhar_no" class="form-control"
                                           t-att-value="employee.spouse_aadhar_no or ''"
                                           readonly="readonly"
                                           data-validation="aadhar"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">12 digits only</small>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Dependent Details Child 1 -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="family">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-child text-primary me-2"></i>Dependent Details (Child 1)
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 Name</label>
                                    <input type="text" name="dependent_child_name_1" class="form-control"
                                           t-att-value="employee.dependent_child_name_1 or ''"
                                           readonly="readonly"/>
                                </div>
                                <!-- CHILD DOB: past date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 DOB</label>
                                    <input type="date" name="dependent_child_dob_1" class="form-control"
                                           t-att-value="employee.dependent_child_dob_1 or ''"
                                           readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 Gender</label>
                                    <select name="dependent_child_gender_1" class="form-select" disabled="disabled">
                                        <option value="">Select Gender</option>
                                        <option value="male"
                                                t-att-selected="'selected' if employee.dependent_child_gender_1 == 'male' else None">
                                            Male
                                        </option>
                                        <option value="female"
                                                t-att-selected="'selected' if employee.dependent_child_gender_1 == 'female' else None">
                                            Female
                                        </option>
                                        <option value="other"
                                                t-att-selected="'selected' if employee.dependent_child_gender_1 == 'other' else None">
                                            Other
                                        </option>
                                    </select>
                                </div>
                                <!-- PASSPORT NO: alphanumeric -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 Passport No</label>
                                    <input type="text" name="dependent_child_passport_no" class="form-control"
                                           t-att-value="employee.dependent_child_passport_no or ''"
                                           readonly="readonly"
                                           data-validation="alphanumeric"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Letters and digits only</small>
                                </div>
                                <!-- PASSPORT ISSUE: past date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 Passport Issue Date</label>
                                    <input type="date" name="dependent_child_passport_issue_date_1" class="form-control"
                                           t-att-value="employee.dependent_child_passport_issue_date_1 or ''"
                                           readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- PASSPORT EXPIRY: future date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 Passport Expiry Date</label>
                                    <input type="date" name="dependent_child_passport_expiry_date_1"
                                           class="form-control"
                                           t-att-value="employee.dependent_child_passport_expiry_date_1 or ''"
                                           readonly="readonly"
                                           data-validation="future-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 Passport Issuing Country</label>
                                    <select name="dependent_child_passport_issuing_countries_1_id"
                                            class="form-select" disabled="disabled">
                                        <option value="">-- Select Country --</option>
                                        <t t-foreach="all_countries" t-as="country">
                                            <option t-att-value="country.id"
                                                    t-att-selected="'selected' if country.id == employee.dependent_child_passport_issuing_countries_1_id.id else None">
                                                <t t-esc="country.name"/>
                                            </option>
                                        </t>
                                    </select>
                                </div>
                                <!-- VISA NO: alphanumeric -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 Visa No</label>
                                    <input type="text" name="dependent_child_visa_no_1" class="form-control"
                                           t-att-value="employee.dependent_child_visa_no_1 or ''"
                                           readonly="readonly"
                                           data-validation="alphanumeric"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Letters and digits only</small>
                                </div>
                                <!-- VISA EXPIRY: future date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 Visa Expiration Date</label>
                                    <input type="date" name="dependent_child_visa_expiration_date_1"
                                           class="form-control"
                                           t-att-value="employee.dependent_child_visa_expiration_date_1 or ''"
                                           readonly="readonly"
                                           data-validation="future-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- EMIRATES ID: format -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 Emirates ID Number</label>
                                    <input type="text" name="dependent_child_emirates_id_no_1" class="form-control"
                                           t-att-value="employee.dependent_child_emirates_id_no_1 or ''"
                                           readonly="readonly"
                                           data-validation="emirates-id"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Format: 784-1234-1234567-1</small>
                                </div>
                                <!-- EMIRATES ISSUE: past date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 Emirates ID Issue Date</label>
                                    <input type="date" name="dependent_child_emirates_id_issue_date_1"
                                           class="form-control"
                                           t-att-value="employee.dependent_child_emirates_id_issue_date_1 or ''"
                                           readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- EMIRATES EXPIRY: future date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 Emirates ID Expiry Date</label>
                                    <input type="date" name="dependent_child_emirates_id_expiry_date_1"
                                           class="form-control"
                                           t-att-value="employee.dependent_child_emirates_id_expiry_date_1 or ''"
                                           readonly="readonly"
                                           data-validation="future-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- AADHAR: 12 digits -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Child 1 Aadhar Number</label>
                                    <input type="text" name="dependent_child_aadhar_no_1" class="form-control"
                                           t-att-value="employee.dependent_child_aadhar_no_1 or ''"
                                           readonly="readonly"
                                           data-validation="aadhar"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">12 digits only</small>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Family -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="family">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-users text-primary me-2"></i>Family
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Disabled</label>
                                    <input class="form-control"
                                           t-att-value="'Yes' if employee.disabled else 'No'"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <!-- DEPENDENT CHILDREN: digits only (integer count) -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Dependent Children</label>
                                    <input name="children" class="form-control"
                                           t-att-value="employee.children or ''"
                                           readonly="readonly"
                                           data-validation="digits-only"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">Digits only</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Marital Status</label>
                                    <select name="marital" class="form-select"
                                            data-always-readonly="1" disabled="disabled">
                                        <option value="">Select Status</option>
                                        <option value="single"
                                                t-att-selected="'selected' if employee.marital == 'single' else None">
                                            Single
                                        </option>
                                        <option value="married"
                                                t-att-selected="'selected' if employee.marital == 'married' else None">
                                            Married
                                        </option>
                                        <option value="cohabitant"
                                                t-att-selected="'selected' if employee.marital == 'cohabitant' else None">
                                            Legal Cohabitant
                                        </option>
                                        <option value="widower"
                                                t-att-selected="'selected' if employee.marital == 'widower' else None">
                                            Widower
                                        </option>
                                        <option value="divorced"
                                                t-att-selected="'selected' if employee.marital == 'divorced' else None">
                                            Divorced
                                        </option>
                                    </select>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Father Name</label>
                                    <input type="text" name="father_name" class="form-control"
                                           t-att-value="employee.father_name or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Father Nationality</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.father_nationalities_id.name if employee.father_nationalities_id else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <!-- FATHER DOB: past date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Father DOB</label>
                                    <input type="date" name="father_dob" class="form-control"
                                           t-att-value="employee.father_dob.strftime('%Y-%m-%d') if employee.father_dob else ''"
                                           readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Dependent Status</label>
                                    <input type="text" name="dependent_status" class="form-control"
                                           t-att-value="employee.dependent_status or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Mother Name</label>
                                    <input type="text" name="mother_name" class="form-control"
                                           t-att-value="employee.mother_name or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Mother Nationality</label>
                                    <input type="text" class="form-control"
                                           t-att-value="employee.mother_nationalities_id.name if employee.mother_nationalities_id else ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <!-- MOTHER DOB: past date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Mother DOB</label>
                                    <input type="date" name="mother_dob" class="form-control"
                                           t-att-value="employee.mother_dob.strftime('%Y-%m-%d') if employee.mother_dob else ''"
                                           readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Dependent Status (1)</label>
                                    <input type="text" name="dependent_status_1" class="form-control"
                                           t-att-value="employee.dependent_status_1 or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Languages -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="family">
                        <div class="card-header bg-light">
                            <h5 class="mb-0">
                                <i class="fa fa-language text-primary me-2"></i>Languages
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Mother Tongue</label>
                                    <input type="text" name="mother_tongue_name" class="form-control"
                                           t-att-value="employee.mother_tongue_name or ''"
                                           readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Languages Known</label>
                                    <input type="text" name="language_known_name" class="form-control"
                                           t-att-value="employee.language_known_name or ''"
                                           readonly="readonly"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- ============================================================ -->
                    <!--  TAB: EDUCATION                                               -->
                    <!-- ============================================================ -->

                    <!-- Employee Education Details -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="education">
                        <div class="card-header bg-light d-flex justify-content-between align-items-center">
                            <h5 class="mb-0">
                                <i class="fa fa-graduation-cap text-primary me-2"></i>Employee Education Details
                            </h5>
                            <div class="form-check form-switch">
                                <input class="form-check-input edit-mode-toggle" type="checkbox"
                                       id="editModeEducation" onchange="syncAndToggleEditMode(this)"/>
                                <label class="form-check-label" for="editModeEducation">Edit Mode</label>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Institution Name</label>
                                    <input type="text" name="institute_name" class="form-control"
                                           t-att-value="employee.institute_name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Degree Name</label>
                                    <input type="text" name="degree_name" class="form-control"
                                           t-att-value="employee.degree_name or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Field of Study</label>
                                    <input type="text" name="field_of_study" class="form-control"
                                           t-att-value="employee.field_of_study or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Study Field (Other)</label>
                                    <input name="study_field" class="form-control"
                                           t-att-value="employee.study_field or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <!-- DEGREE START: past date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Start Date of Degree</label>
                                    <input type="date" name="start_date_of_degree" class="form-control"
                                           t-att-value="employee.start_date_of_degree.strftime('%Y-%m-%d') if employee.start_date_of_degree else ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- DEGREE COMPLETION: past date -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Completion Date of Degree</label>
                                    <input type="date" name="completion_date_of_degree" class="form-control"
                                           t-att-value="employee.completion_date_of_degree.strftime('%Y-%m-%d') if employee.completion_date_of_degree else ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="past-date"/>
                                    <div class="invalid-feedback"></div>
                                </div>
                                <!-- YEAR OF PASSING: 4-digit year -->
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Year of Passing</label>
                                    <input type="text" name="year_of_passing" class="form-control"
                                           t-att-value="employee.year_of_passing or ''"
                                           data-always-readonly="1" readonly="readonly"
                                           data-validation="year"/>
                                    <div class="invalid-feedback"></div>
                                    <small class="text-muted">4-digit year e.g. 2020</small>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Score</label>
                                    <input type="text" name="score" class="form-control"
                                           t-att-value="employee.score or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Degree Certificate Legalisation</label>
                                    <input type="text" class="form-control"
                                           t-att-value="'Yes' if employee.degree_certificate_legal else 'No'"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-bold">Certification Obtained</label>
                                    <input type="text" name="certification_obtained" class="form-control"
                                           t-att-value="employee.certification_obtained or ''"
                                           data-always-readonly="1" readonly="readonly"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- ============================================================ -->
                    <!--  TAB: DOCUMENTS                                               -->
                    <!-- ============================================================ -->

                    <!-- Document Management -->
                    <div class="card mb-4 shadow-sm profile-tab-section" data-tab-group="documents">
                        <div class="card-header bg-light d-flex justify-content-between align-items-center">
                            <h5 class="mb-0">
                                <i class="fa fa-folder-open text-primary me-2"></i>Document Management
                            </h5>
                            <div class="form-check form-switch">
                                <input class="form-check-input edit-mode-toggle" type="checkbox"
                                       id="editModeDocuments" onchange="syncAndToggleEditMode(this)"/>
                                <label class="form-check-label" for="editModeDocuments">Edit Mode</label>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="row g-3">
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-id-card text-muted me-1"></i>Emirates ID Copy
                                    </label>
                                    <t t-if="employee.emirates_id_file">
                                        <div class="mb-2">
                                            <a t-att-href="'/web/content/hr.employee/%s/emirates_id_file?download=true' % employee.id"
                                               class="btn btn-sm btn-outline-primary" target="_blank">
                                                <i class="fa fa-download me-1"></i>Download File
                                            </a>
                                        </div>
                                    </t>
                                    <input type="file" name="emirates_id_file" class="form-control"
                                           accept=".pdf,.jpg,.jpeg,.png"/>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-passport text-muted me-1"></i>Passport Copy
                                    </label>
                                    <t t-if="employee.passport_file">
                                        <div class="mb-2">
                                            <a t-att-href="'/web/content/hr.employee/%s/passport_file?download=true' % employee.id"
                                               class="btn btn-sm btn-outline-primary" target="_blank">
                                                <i class="fa fa-download me-1"></i>Download File
                                            </a>
                                        </div>
                                    </t>
                                    <input type="file" name="passport_file" class="form-control"
                                           accept=".pdf,.jpg,.jpeg,.png"/>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-file-text text-muted me-1"></i>Other Documents
                                    </label>
                                    <t t-if="employee.other_documents">
                                        <div class="mb-2">
                                            <a t-att-href="'/web/content/hr.employee/%s/other_documents?download=true' % employee.id"
                                               class="btn btn-sm btn-outline-primary" target="_blank">
                                                <i class="fa fa-download me-1"></i>Download File
                                            </a>
                                        </div>
                                    </t>
                                    <input type="file" name="other_documents" class="form-control"
                                           accept=".pdf,.jpg,.jpeg,.png"/>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label fw-bold">
                                        <i class="fa fa-file text-muted me-1"></i>Work Permit
                                    </label>
                                    <t t-if="employee.has_work_permit">
                                        <div class="mb-2">
                                            <a t-att-href="'/web/content/hr.employee/%s/has_work_permit?download=true' % employee.id"
                                               target="_blank" class="btn btn-sm btn-outline-primary">
                                                <i class="fa fa-download me-1"></i>Download File
                                            </a>
                                        </div>
                                    </t>
                                    <input type="file" name="has_work_permit" class="form-control"
                                           accept=".pdf,.jpg,.jpeg,.png"/>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- ===== FORM ACTIONS ===== -->
                    <div class="card shadow-sm">
                        <div class="card-body text-center">
                            <div id="formActions" style="display: none;">
                                <button type="button" class="btn btn-secondary me-2" onclick="cancelEdit()">
                                    <i class="fa fa-times me-1"></i>Cancel
                                </button>
                                <button type="button" class="btn btn-info me-2" onclick="saveAsDraft()">
                                    <i class="fa fa-save me-1"></i>Save as Draft
                                </button>
                                <button type="submit" class="btn btn-primary">
                                    <i class="fa fa-check me-1"></i>Save Changes
                                </button>
                            </div>
                            <div id="viewActions">
                                <p class="text-muted mb-3">
                                    <i class="fa fa-info-circle me-1"></i>
                                    Profile last updated:
                                    <strong>
                                        <t t-esc="employee.write_date.strftime('%d %b, %Y at %I:%M %p') if employee.write_date else 'Never'"/>
                                    </strong>
                                </p>
                                <button type="button" class="btn btn-outline-primary" onclick="enableEditMode()">
                                    <i class="fa fa-edit me-1"></i>Edit Profile
                                </button>
                            </div>
                        </div>
                    </div>

                </form>

                <!-- ===== JAVASCRIPT ===== -->
                <script type="text/javascript">
                    <![CDATA[
let isEditMode = false;
let originalFormData = {};
let activeProfileTab = 'basic';

/* ─── Tab switching ─────────────────────────────────────────── */
function switchProfileTab(tabKey) {
    const previousTab = activeProfileTab;
    activeProfileTab = tabKey;
    if (previousTab && previousTab !== tabKey) {
        clearValidationStates(previousTab);
    }
    document.querySelectorAll('#personalDetailsForm .profile-tab-section').forEach(card => {
        card.style.display = card.getAttribute('data-tab-group') === tabKey ? '' : 'none';
    });
    document.querySelectorAll('#personalDetailsForm .profile-tab-button').forEach(btn => {
        const active = btn.getAttribute('data-tab-target') === tabKey;
        btn.classList.toggle('active', active);
        btn.setAttribute('aria-selected', active ? 'true' : 'false');
    });
}

/* ─── Sync all toggles ──────────────────────────────────────── */
function syncAndToggleEditMode(sourceCheckbox) {
    document.querySelectorAll('.edit-mode-toggle').forEach(t => {
        t.checked = sourceCheckbox.checked;
    });
    sourceCheckbox.checked ? enableEditMode() : disableEditMode();
}

/* ─── Enable edit mode ──────────────────────────────────────── */
function enableEditMode() {
    isEditMode = true;
    document.querySelectorAll('.edit-mode-toggle').forEach(t => t.checked = true);
    storeOriginalFormData();
    document.querySelectorAll('#personalDetailsForm input, #personalDetailsForm select').forEach(input => {
        if (input.type === 'hidden') return;
        if (input.classList.contains('edit-mode-toggle')) return;
        if (input.hasAttribute('data-always-readonly')) {
            input.setAttribute('readonly', 'readonly');
            if (input.tagName === 'SELECT' || input.type === 'checkbox') input.setAttribute('disabled', 'disabled');
            input.style.setProperty('pointer-events', 'none');
            input.style.setProperty('background-color', '#e9ecef');
            return;
        }
        input.removeAttribute('readonly');
        input.removeAttribute('disabled');
        input.style.removeProperty('pointer-events');
        input.style.removeProperty('background-color');
    });
    document.getElementById('formActions').style.display = 'block';
    document.getElementById('viewActions').style.display = 'none';
    document.querySelectorAll('#personalDetailsForm input, #personalDetailsForm select').forEach(input => {
        input.removeEventListener('input', validateField);
        input.removeEventListener('blur', validateField);
        input.addEventListener('input', validateField);
        input.addEventListener('blur', validateField);
    });
}

/* ─── Disable edit mode ─────────────────────────────────────── */
function disableEditMode() {
    isEditMode = false;
    document.querySelectorAll('.edit-mode-toggle').forEach(t => t.checked = false);
    document.querySelectorAll('#personalDetailsForm input, #personalDetailsForm select').forEach(input => {
        if (input.type === 'hidden') return;
        if (input.classList.contains('edit-mode-toggle')) return;
        if (input.hasAttribute('data-always-readonly')) {
            input.setAttribute('readonly', 'readonly');
            if (input.tagName === 'SELECT' || input.type === 'checkbox') input.setAttribute('disabled', 'disabled');
            input.style.setProperty('pointer-events', 'none');
            input.style.setProperty('background-color', '#e9ecef');
            return;
        }
        input.setAttribute('readonly', 'readonly');
        if (input.tagName === 'SELECT' || input.type === 'checkbox') input.setAttribute('disabled', 'disabled');
        input.style.removeProperty('pointer-events');
        input.style.removeProperty('background-color');
    });
    document.getElementById('formActions').style.display = 'none';
    document.getElementById('viewActions').style.display = 'block';
    clearValidationStates(activeProfileTab);
}

/* ─── Store original data ───────────────────────────────────── */
function storeOriginalFormData() {
    const formData = new FormData(document.getElementById('personalDetailsForm'));
    originalFormData = {};
    for (let [key, value] of formData.entries()) originalFormData[key] = value;
}

/* ─── Cancel ────────────────────────────────────────────────── */
function cancelEdit() {
    for (let [key, value] of Object.entries(originalFormData)) {
        const input = document.querySelector(`[name="${key}"]`);
        if (input) input.value = value;
    }
    clearValidationStates(activeProfileTab);
    disableEditMode();
    showTabMessage(activeProfileTab, 'Changes cancelled.', 'info');
}

/* ─── Save as draft ─────────────────────────────────────────── */
function saveAsDraft() {
    const formData = new FormData(document.getElementById('personalDetailsForm'));
    const draftData = {};
    for (let [key, value] of formData.entries()) draftData[key] = value;
    localStorage.setItem('profileDraft', JSON.stringify(draftData));
    showTabMessage(activeProfileTab, 'Profile saved as draft.', 'info');
}

/* ─── Validate single field ─────────────────────────────────── */
function validateField(event) {
    const field = event.target;

    // Only validate fields in the currently active tab
    const parentSection = field.closest('.profile-tab-section');
    if (parentSection && parentSection.getAttribute('data-tab-group') !== activeProfileTab) {
        return true;
    }

    const validationType = field.getAttribute('data-validation');
    let isValid = true;
    let errorMessage = '';

    field.classList.remove('is-valid', 'is-invalid');

    // Remove previously injected dynamic error
    const existingDynamic = field.parentNode.querySelector('.invalid-feedback.dynamic-error');
    if (existingDynamic) existingDynamic.remove();

    // Clear static feedback
    const feedback = field.parentNode.querySelector('.invalid-feedback:not(.dynamic-error)');
    if (feedback) {
        feedback.textContent = '';
        feedback.style.display = 'none';
    }

    if (validationType && field.value) {
        const val = field.value.trim();

        switch (validationType) {

            /* ── EMAIL ── */
            case 'email':
                if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(val)) {
                    isValid = false;
                    errorMessage = 'Please enter a valid email address (e.g. name@example.com).';
                }
                break;

            /* ── PHONE: digits only, optional leading + ── */
            case 'phone':
                const phoneCleaned = val.replace(/\s/g, '');
                if (!/^\+?[0-9]+$/.test(phoneCleaned)) {
                    isValid = false;
                    errorMessage = 'Phone number must contain digits only. A leading + is allowed for country code (e.g. +971501234567).';
                } else {
                    const digitsOnly = phoneCleaned.replace('+', '');
                    if (digitsOnly.length < 7 || digitsOnly.length > 15) {
                        isValid = false;
                        errorMessage = 'Phone number must be between 7 and 15 digits.';
                    }
                }
                break;

            /* ── DIGITS ONLY: pure integers, no letters ── */
            case 'digits-only':
                if (!/^[0-9]+$/.test(val)) {
                    isValid = false;
                    errorMessage = 'This field must contain digits only (no letters or special characters).';
                }
                break;

            /* ── ALPHANUMERIC: letters and digits, no spaces or special chars ── */
            case 'alphanumeric':
                if (!/^[A-Za-z0-9]+$/.test(val)) {
                    isValid = false;
                    errorMessage = 'This field must contain letters and digits only (no spaces or special characters).';
                }
                break;

            /* ── EMIRATES ID: 784-XXXX-XXXXXXX-X ── */
            case 'emirates-id':
                if (!/^[0-9]{3}-[0-9]{4}-[0-9]{7}-[0-9]{1}$/.test(val)) {
                    isValid = false;
                    errorMessage = 'Emirates ID must be in format: 784-1234-1234567-1';
                }
                break;

            /* ── AADHAR: exactly 12 digits ── */
            case 'aadhar':
                if (!/^[0-9]{12}$/.test(val)) {
                    isValid = false;
                    errorMessage = 'Aadhar number must be exactly 12 digits.';
                }
                break;

            /* ── PAN: 5 letters + 4 digits + 1 letter = 10 chars ── */
            case 'pan':
                if (!/^[A-Z]{5}[0-9]{4}[A-Z]{1}$/.test(val.toUpperCase())) {
                    isValid = false;
                    errorMessage = 'PAN must be in format: ABCDE1234F (5 letters, 4 digits, 1 letter).';
                }
                break;

            /* ── YEAR: 4-digit year, not in future ── */
            case 'year':
                if (!/^[0-9]{4}$/.test(val)) {
                    isValid = false;
                    errorMessage = 'Please enter a valid 4-digit year (e.g. 2020).';
                } else {
                    const yr = parseInt(val, 10);
                    const currentYear = new Date().getFullYear();
                    if (yr < 1900 || yr > currentYear) {
                        isValid = false;
                        errorMessage = `Year must be between 1900 and ${currentYear}.`;
                    }
                }
                break;

            /* ── PAST DATE: must be today or earlier ── */
            case 'past-date':
                const pastInput = new Date(val);
                const todayForPast = new Date();
                todayForPast.setHours(0, 0, 0, 0);
                if (isNaN(pastInput.getTime())) {
                    isValid = false;
                    errorMessage = 'Please enter a valid date.';
                } else if (pastInput > todayForPast) {
                    isValid = false;
                    errorMessage = 'This date cannot be in the future.';
                }
                break;

            /* ── FUTURE DATE: must be after today ── */
            case 'future-date':
                const futureInput = new Date(val);
                const todayForFuture = new Date();
                todayForFuture.setHours(0, 0, 0, 0);
                if (isNaN(futureInput.getTime())) {
                    isValid = false;
                    errorMessage = 'Please enter a valid date.';
                } else if (futureInput <= todayForFuture) {
                    isValid = false;
                    errorMessage = 'Expiry date must be a future date. Entered: ' + futureInput.toDateString() + ' | Today: ' + todayForFuture.toDateString();
                }
                break;

            /* ── REQUIRED ── */
            case 'required':
                if (!val) {
                    isValid = false;
                    errorMessage = 'This field is required.';
                }
                break;
        }
    }

    if (field.value.trim()) {
        if (isValid) {
            field.classList.add('is-valid');
        } else {
            field.classList.add('is-invalid');
            if (feedback) {
                feedback.textContent = errorMessage;
                feedback.style.display = 'block';
            } else {
                const errDiv = document.createElement('div');
                errDiv.className = 'invalid-feedback dynamic-error';
                errDiv.style.display = 'block';
                errDiv.style.color = '#dc3545';
                errDiv.style.fontSize = '0.8rem';
                errDiv.style.marginTop = '3px';
                errDiv.textContent = errorMessage;
                field.insertAdjacentElement('afterend', errDiv);
            }
        }
    }

    updateTabErrorBadges();
    return isValid;
}

/* ─── Tab error badges ──────────────────────────────────────── */
function updateTabErrorBadges() {
    ['basic', 'professional', 'family', 'education', 'documents'].forEach(tabKey => {
        const tabBtn = document.querySelector(`.profile-tab-button[data-tab-target="${tabKey}"]`);
        if (!tabBtn) return;
        const existing = tabBtn.querySelector('.tab-error-badge');
        if (existing) existing.remove();
        const count = document.querySelectorAll(
            `.profile-tab-section[data-tab-group="${tabKey}"] .is-invalid`
        ).length;
        if (count > 0) {
            const badge = document.createElement('span');
            badge.className = 'tab-error-badge badge bg-danger ms-1';
            badge.textContent = count;
            tabBtn.appendChild(badge);
        }
    });
}

/* ─── Clear validation states — scoped to a tab ────────────── */
function clearValidationStates(tabKey) {
    let inputs = [], feedbacks = [], messages = [];
    if (tabKey) {
        const sections = document.querySelectorAll(`.profile-tab-section[data-tab-group="${tabKey}"]`);
        sections.forEach(section => {
            section.querySelectorAll('input, select').forEach(el => inputs.push(el));
            section.querySelectorAll('.invalid-feedback').forEach(el => feedbacks.push(el));
            section.querySelectorAll('.tab-message-box').forEach(el => messages.push(el));
            section.querySelectorAll('.dynamic-error').forEach(el => el.remove());
        });
    } else {
        inputs = document.querySelectorAll('#personalDetailsForm input, #personalDetailsForm select');
        feedbacks = document.querySelectorAll('#personalDetailsForm .invalid-feedback');
        messages = document.querySelectorAll('.tab-message-box');
        document.querySelectorAll('.dynamic-error').forEach(el => el.remove());
    }
    inputs.forEach(input => input.classList.remove('is-valid', 'is-invalid'));
    feedbacks.forEach(el => { el.textContent = ''; el.style.display = 'none'; });
    messages.forEach(el => el.remove());
    updateTabErrorBadges();
}

/* ─── Show message inside active tab ───────────────────────── */
function showTabMessage(tabKey, text, type) {
    document.querySelectorAll('.tab-message-box').forEach(el => el.remove());
    const firstCard = document.querySelector(`.profile-tab-section[data-tab-group="${tabKey}"]`);
    if (!firstCard) return;
    const alertClass = type === 'success' ? 'alert-success' : type === 'info' ? 'alert-info' : 'alert-danger';
    const icon = type === 'success' ? 'fa-check-circle' : type === 'info' ? 'fa-info-circle' : 'fa-exclamation-triangle';
    const msgDiv = document.createElement('div');
    msgDiv.className = `tab-message-box alert ${alertClass} alert-dismissible fade show mb-3`;
    msgDiv.innerHTML = `
        <i class="fa ${icon} me-2"></i>
        <span>${text}</span>
        <button type="button" class="btn-close" onclick="this.parentNode.remove()"></button>
    `;
    firstCard.parentNode.insertBefore(msgDiv, firstCard);
    msgDiv.scrollIntoView({ behavior: 'smooth', block: 'center' });
    setTimeout(() => { if (msgDiv.parentNode) msgDiv.remove(); }, 5000);
}

/* ─── Form submit ───────────────────────────────────────────── */
document.getElementById('personalDetailsForm').addEventListener('submit', function(e) {
    e.preventDefault();
    clearValidationStates(activeProfileTab);

    let isFormValid = true;
    document.querySelectorAll(
        `.profile-tab-section[data-tab-group="${activeProfileTab}"] input[data-validation],
         .profile-tab-section[data-tab-group="${activeProfileTab}"] select[data-validation]`
    ).forEach(input => {
        if (!validateField({ target: input })) isFormValid = false;
    });

    updateTabErrorBadges();

    if (!isFormValid) {
        const firstError = document.querySelector(
            `.profile-tab-section[data-tab-group="${activeProfileTab}"] .is-invalid`
        );
        if (firstError) {
            setTimeout(() => { firstError.scrollIntoView({ behavior: 'smooth', block: 'center' }); firstError.focus(); }, 100);
        }
        showTabMessage(activeProfileTab, 'Please correct the highlighted errors in this tab.', 'error');
        return;
    }

    const toReenable = [];
    document.querySelectorAll(
        '#personalDetailsForm select[disabled], #personalDetailsForm input[type="checkbox"][disabled]'
    ).forEach(el => {
        if (!el.hasAttribute('data-always-readonly')) {
            el.removeAttribute('disabled');
            toReenable.push(el);
        }
    });

    const formData = new FormData(this);
    const csrfToken = document.querySelector('input[name="csrf_token"]').value;
    toReenable.forEach(el => el.setAttribute('disabled', 'disabled'));

    fetch('/my/employee/personal', {
        method: 'POST',
        body: formData,
        headers: { 'X-CSRFToken': csrfToken }
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            showTabMessage(activeProfileTab, 'Profile updated successfully!', 'success');
            disableEditMode();
            localStorage.removeItem('profileDraft');
        } else {
            showTabMessage(activeProfileTab, 'Error: ' + (data.error || 'Unknown error'), 'error');
        }
    })
    .catch(error => {
        console.error('Error:', error);
        showTabMessage(activeProfileTab, 'Network error. Please try again.', 'error');
    });
});

/* ─── DOMContentLoaded ──────────────────────────────────────── */
document.addEventListener('DOMContentLoaded', function() {
    document.querySelectorAll('#personalDetailsForm .profile-tab-button').forEach(button => {
        button.addEventListener('click', function() {
            switchProfileTab(this.getAttribute('data-tab-target'));
        });
    });
    switchProfileTab(activeProfileTab);

    const draftData = localStorage.getItem('profileDraft');
    if (draftData) {
        try {
            const draft = JSON.parse(draftData);
            Object.entries(draft).forEach(([key, value]) => {
                const input = document.querySelector(`[name="${key}"]`);
                if (input && input.type !== 'hidden') input.value = value;
            });
            showTabMessage(activeProfileTab, 'Draft data loaded.', 'info');
        } catch (e) {
            console.error('Error loading draft:', e);
        }
    }
});
                    ]]>
                </script>

                <!-- ===== CSS ===== -->
                <style>
                    <![CDATA[
.form-label { font-weight: 600; color: #495057; margin-bottom: 0.5rem; }

.profile-tab-navigation .nav-link {
    border-radius: 0.5rem 0.5rem 0 0;
    font-weight: 600;
    color: #495057;
    transition: color 0.2s;
}
.profile-tab-navigation .nav-link.active { color: #0d6efd; }

.tab-error-badge {
    font-size: 0.65rem;
    border-radius: 50%;
    padding: 2px 6px;
    vertical-align: middle;
    animation: badgePop 0.2s ease;
}
@keyframes badgePop {
    0%   { transform: scale(0); }
    80%  { transform: scale(1.2); }
    100% { transform: scale(1); }
}

.form-control.is-invalid,
.form-select.is-invalid {
    border-color: #dc3545 !important;
    box-shadow: 0 0 0 0.2rem rgba(220,53,69,0.2) !important;
    animation: shake 0.35s ease;
}
@keyframes shake {
    0%,100% { transform: translateX(0); }
    20%      { transform: translateX(-5px); }
    40%      { transform: translateX(5px); }
    60%      { transform: translateX(-3px); }
    80%      { transform: translateX(3px); }
}
.form-control.is-valid { border-color: #198754; }

.is-invalid ~ .invalid-feedback,
.form-select.is-invalid ~ .invalid-feedback {
    display: block !important;
    color: #dc3545;
    font-size: 0.8rem;
    margin-top: 3px;
}

.form-control:focus,
.form-select:focus {
    border-color: #80bdff;
    box-shadow: 0 0 0 0.2rem rgba(13,110,253,0.25);
}

.card { transition: box-shadow 0.3s ease; }
.card:hover { box-shadow: 0 4px 15px rgba(0,0,0,0.1) !important; }

.form-switch .form-check-input { background-color: #6c757d; }
.form-switch .form-check-input:checked { background-color: #0d6efd; border-color: #0d6efd; }

.tab-message-box { border-radius: 0.5rem; font-weight: 500; }

@media (max-width: 768px) {
    .card-body { padding: 1rem; }
    .row.g-3 > * { margin-bottom: 1rem; }
    .profile-tab-navigation .nav-link { font-size: 0.85rem; padding: 0.4rem 0.6rem; }
}
                    ]]>
                </style>
            </t>
        </t>
    </template>
</odoo>